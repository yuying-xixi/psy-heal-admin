<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.psy.mapper.CourseCollectMapper">
    
    <resultMap type="CourseCollect" id="CourseCollectResult">
        <result property="id"    column="id"    />
        <result property="courseId"    column="course_id"    />
        <result property="userId"    column="user_id"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectCourseCollectVo">
        select id, course_id, user_id, create_time from course_collect
    </sql>

    <select id="selectCourseCollectList" parameterType="CourseCollect" resultMap="CourseCollectResult">
        <include refid="selectCourseCollectVo"/>
        <where>  
            <if test="courseId != null "> and course_id = #{courseId}</if>
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="params.beginCreateTime != null and params.beginCreateTime != '' and params.endCreateTime != null and params.endCreateTime != ''"> and create_time between #{params.beginCreateTime} and #{params.endCreateTime}</if>
        </where>
    </select>
    
    <select id="selectCourseCollectById" parameterType="Long" resultMap="CourseCollectResult">
        <include refid="selectCourseCollectVo"/>
        where id = #{id}
    </select>

    <insert id="insertCourseCollect" parameterType="CourseCollect" useGeneratedKeys="true" keyProperty="id">
        insert into course_collect
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="courseId != null">course_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="courseId != null">#{courseId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateCourseCollect" parameterType="CourseCollect">
        update course_collect
        <trim prefix="SET" suffixOverrides=",">
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteCourseCollectById" parameterType="Long">
        delete from course_collect where id = #{id}
    </delete>

    <delete id="deleteCourseCollectByIds" parameterType="String">
        delete from course_collect where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>